language: python
python:
    - "2.6"
    - "2.7"
    - "3.2"
    - "3.3"
    - "pypy"
install:
    - pip install nose
    - python setup.py install
script:
    - nosetests -v
    - nosetests -v --with-doctest --doctest-extension=rst
